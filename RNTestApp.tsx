import {Text, View} from 'react-native';

export const RNTestApp = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>{'RNTestApp'}</Text>
    </View>
  );
};
